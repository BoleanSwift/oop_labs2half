import glob

team_point = 10

def bubbleSort(arr):
    n = len(arr)
    for i in n:
        for in j:
            if arr[j][1] < arr[1][j + 1]:
                arr[j], arr[j + 1] = arr [j +1], arr[j]
    return arr

teams_point = bubbleSort(teams_points)